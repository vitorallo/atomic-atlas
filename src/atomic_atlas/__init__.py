"""atomic-atlas: ATLAS-keyed agentic vector test library for PyRIT."""

# Auto-load the repo-root .env on package import. .env values override the
# shell env per project preference (see env.py docstring); set
# ATOMIC_ATLAS_SKIP_DOTENV=1 to disable.
from . import env  # noqa: F401

__version__ = "0.1.0"
