# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
*.egg
build/
dist/
wheels/
.eggs/

# Virtual environments
.venv/
venv/
env/
ENV/

# Testing & tooling caches
.pytest_cache/
.tox/
.nox/
.coverage
.coverage.*
htmlcov/
.mypy_cache/
.ruff_cache/

# Type-checker / IDE
.pyre/
.pytype/
.idea/
.vscode/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Slide authoring sources — track the exported PDF, not the editable deck
*.pptx
*.key
~$*

# Secrets & local config
.env
.env.*
!.env.example

# atomic-atlas runtime artifacts
results.json
*.results.json
runbook-results.json
*-runbook-results.json
layer.json
*.layer.json
atomic-atlas-engagement/
*.engagement/

# Curated showcase assessment — committed on purpose; un-ignore it so the
# global artifact patterns above (layer.json, *.results.json, …) don't eat it.
!sample_assessment1/
!sample_assessment1/**

# PyRIT memory (default DB path written by SQLiteMemory when not :memory:)
pyrit.db
*.db
*.duckdb

# Claude Code / agent assistant workspace state — not project source.
.claude/
CLAUDE.md
.claude-context/
